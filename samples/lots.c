#include <stdio.h>

int lots(void) {
	int a, b, c, d, e, f, g, h, i, j, k, l;

	printf("%d%d%d%d%d%d%d%d%d%d%d%d",
		a, b, c, d, e, f, g, h, i, j, k, l);

	return 0;
}

int main() {
	lots();

	return 0;
}